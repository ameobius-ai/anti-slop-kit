Write the user-facing text for three error states of a file upload feature: the
file is too large, the file type is not supported, and the connection dropped
mid-upload. Each error needs a cause and a next action.
