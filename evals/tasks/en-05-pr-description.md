Write a pull request description for a change that replaces a polling loop with
a webhook listener in a payment reconciliation service. Include the reason, the
risk, and how a reviewer should verify it.
